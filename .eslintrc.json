{
  "extends": [
    "next/core-web-vitals",
    "next/typescript"
  ],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "JSXAttribute[name.name='className'][value.type='Literal']",
        "message": "Inline Tailwind classes are not allowed. Define classes in a CSS/SCSS module and use @apply."
      }
    ]
  }
}
